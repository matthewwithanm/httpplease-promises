/*jshint strict:false */
/*globals describe, it */

var
    assert = require('chai').assert,
    http = require('httpplease'),
    promises = require('../lib/index'),
    testServerUrl = 'http://localhost:4001';


describe('httpplease-promises', function () {
    it('passes a test', function () {
        assert(true);
    });
});
